[development]
# The 'default' name is for one project name in the folder sites also is the
# only one name that works with nginx when connecting from localhost
localhost ansible_ssh_host=127.0.0.1 ansible_ssh_port=50778 ansible_ssh_private_key_file=../development_tools/.vagrant/machines/default/virtualbox/private_key
